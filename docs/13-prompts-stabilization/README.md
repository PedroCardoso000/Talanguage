# Tala Language — Stabilization Prompts

Use esta trilha depois dos prompts Spring Boot fix.

Objetivo: corrigir os problemas funcionais restantes sem recriar o projeto, preservando a UI existente e conectando as funcionalidades ao backend Spring Boot e banco real.

Regra principal: cada prompt deve alterar somente o necessário para resolver o problema indicado.

Ordem: execute de `035` até `047`.
